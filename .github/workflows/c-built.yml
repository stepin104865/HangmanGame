name: C/C++ CI
on:[push]
  
 jobs:
  build:
    name: c/c++ CI
    runs-on: ubuntu-latest
     steps:
    - uses: actions/checkout@v2
      
    - name: make all
      run:  make all -C 3_Implementation/
